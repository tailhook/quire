Quire Raw Preprocessing
=======================

This dir contains preprocessing code that applies to raw YAML, in particular:

* Processing of ``<<`` keys
* Processing of ``!Include``, ``!FromFile`` and similar directives
